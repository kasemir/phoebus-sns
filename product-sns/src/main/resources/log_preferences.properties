# --------------------------------------
# Package org.phoebus.sns.logbook
# --------------------------------------

# URL to Logbook Oracle RDB
rdb_url=jdbc:oracle:thin:@(DESCRIPTION=(LOAD_BALANCE=OFF)(FAILOVER=ON)(ADDRESS=(PROTOCOL=TCP)(HOST=snsappa.sns.ornl.gov)(PORT=1610))(ADDRESS=(PROTOCOL=TCP)(HOST=snsappb.sns.ornl.gov)(PORT=1610))(CONNECT_DATA=(SERVICE_NAME=prod_controls)))

# Username for read only RDB account.
read_only_username=sns_reports
# Password for read only RDB account.
read_only_password=sns